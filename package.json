{
  "name": "express-pug-bootstrap-starter-template",
  "version": "0.1.0",
  "description": "A starter template for a web app using express, pug and bootstrap",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "lint": "standard && prettier .",
    "lint:fix": "prettier --write . && standard --fix"
  },
  "author": "",
  "license": "Unlicense",
  "dependencies": {
    "bootstrap": "^5.3.2",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "@playwright/test": "^1.40.0",
    "prettier": "3.1.0",
    "standard": "^17.1.0"
  }
}
